node_modules/
dist/
*.tsbuildinfo

# scratch output from running the CI assertions locally
s.json
board.json
graph.json

# `make demo` scratch: the synthetic machine, the frames, the encoder's logs
.demo/
